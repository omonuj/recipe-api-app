# Core framework
Django>=4.2.0,<4.3.0
djangorestframework>=3.15.0,<3.16.0

# OpenAPI 3 / Swagger documentation
drf-spectacular>=0.27.0,<0.28.0

# Database driver (PostgreSQL)
# psycopg2-binary ships prebuilt wheels — required for the Vercel build
# environment (no Postgres C headers) and works in Docker too.
psycopg2-binary>=2.9.5,<2.10.0

# Image handling for recipe uploads
Pillow>=10.2.0,<11.0.0

# Deployment (Vercel / production)
dj-database-url>=2.1.0,<3.0.0
whitenoise>=6.6.0,<7.0.0

# Linting (development)
flake8>=7.0.0,<7.1.0
